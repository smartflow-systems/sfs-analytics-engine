# SFS Analytics Engine - Environment Configuration

# Database
# PostgreSQL connection string (required for production)
# For local development, you can use a local PostgreSQL instance or Neon serverless
DATABASE_URL=postgresql://user:password@localhost:5432/sfs_analytics

# Server
# Port for the Express server (default: 5000)
PORT=5000

# Node Environment
NODE_ENV=development

# Redis (optional, for event queuing)
# If not set, events will be inserted directly to the database
# REDIS_URL=redis://localhost:6379

# Session (optional, for authentication)
# SESSION_SECRET=your-secret-key-here

# Monitoring & Alerts (optional)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
# ALERT_EMAIL=alerts@yourcompany.com

# Multi-tenancy (optional)
# DEMO_WORKSPACE_ID=demo-workspace-001

# Rate Limiting (optional)
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=1000
